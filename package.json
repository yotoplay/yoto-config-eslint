{
  "name": "@yoto/eslint-config",
  "type": "module",
  "version": "3.0.40",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "deploy:patch": "npm version patch && git push --tags",
    "deploy:minor": "npm version minor && git push --tags",
    "deploy:major": "npm version major && git push --tags",
    "publish": "standard-version",
    "skip-ci": "git commit --amend --no-edit -m \"chore(release): $(node -p \"require('./package.json').version\") [skip ci]\""
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "eslint-config-prettier": "*",
    "eslint-plugin-unicorn": "*"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6",
    "semver": "^7.6.3",
    "standard-version": "^9.5.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "type-enum": [
        2,
        "always",
        [
          "feat",
          "fix",
          "docs",
          "style",
          "refactor",
          "test",
          "chore"
        ]
      ]
    }
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test",
      "commit-msg": "commitlint --edit $1"
    }
  }
}
